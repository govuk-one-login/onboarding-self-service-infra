{
  "name": "sns-to-slack",
  "description": "Take an SNS message and send it to Slack via a webhook",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "axios": "^1.12.2",
    "esbuild": "^0.25.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.134"
  }
}
